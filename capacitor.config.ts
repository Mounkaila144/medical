import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
appId: 'com.medical.ptrniger.com',
  appName: 'Gestion-Medical',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
