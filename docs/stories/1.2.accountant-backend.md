# Story 1.2: Ajouter le role ACCOUNTANT au backend

## Status: Ready for Review

## Story

**As a** CLINIC_ADMIN,
**I want** pouvoir creer un utilisateur avec le role ACCOUNTANT,
**so that** le comptable de ma clinique ait un compte dedie.

## Acceptance Criteria

1. Le role `ACCOUNTANT` est ajoute a l'enum `AuthUserRole`
2. Les controllers billing (invoices, payments, expenses, tariffs) incluent `ACCOUNTANT` dans leurs `@Roles()`
3. Le `ACCOUNTANT` a acces en lecture/ecriture aux factures, paiements, depenses
4. Le `ACCOUNTANT` a acces en lecture aux tarifs
5. Le `ACCOUNTANT` n'a PAS acces aux endpoints patients, encounters, prescriptions, lab-results
6. Le `ACCOUNTANT` peut voir les stats de depenses (`GET /expenses/stats`)
7. Le `CreateUserDto` accepte le role `ACCOUNTANT`

## Tasks / Subtasks

- [x] **Task 1 : Mettre a jour les controllers billing** (AC: 2, 3, 4) — deja fait
- [x] **Task 2 : Verifier que ACCOUNTANT n'a PAS acces aux endpoints cliniques** (AC: 5) — verifie
- [x] **Task 3 : Mettre a jour le CreateUserDto** (AC: 7) — utilise AuthUserRole enum
- [x] **Task 4 : Verification** (AC: 1-7) — tous les @Roles() corrects

## Dev Notes

### Prerequis

Story 1.1 doit etre completee (enum `AuthUserRole` avec `ACCOUNTANT` deja ajoute).

### Fichiers a modifier

- `medicalBackend/src/billing/controllers/invoices.controller.ts` — 11 `@Roles()` decorators
- `medicalBackend/src/billing/controllers/payments.controller.ts` — 5 `@Roles()` decorators
- `medicalBackend/src/billing/controllers/expenses.controller.ts` — 6 `@Roles()` decorators
- `medicalBackend/src/billing/controllers/tariffs.controller.ts` — 6 `@Roles()` decorators (GET seulement)

### Regles d'acces ACCOUNTANT

| Controller | GET | POST | PATCH | DELETE |
|-----------|-----|------|-------|--------|
| Invoices | OUI | OUI | OUI | NON |
| Payments | OUI | OUI | OUI | NON |
| Expenses | OUI + stats | OUI | OUI | NON |
| Tariffs | OUI | NON | NON | NON |

### Pattern existant

Les `@Roles()` sont deja utilises sur chaque methode de controller. Il suffit d'ajouter `AuthUserRole.ACCOUNTANT` dans les listes existantes. [Source: architecture.md#api-design]

### Testing

- **Framework :** Jest (backend)
- **Verification :** Tester manuellement le login d'un ACCOUNTANT et verifier les acces aux endpoints billing (200) et patients (403).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-11 | 1.0 | Creation initiale | Bob (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### Debug Log References
Aucun bug

### Completion Notes List
- Tous les controllers billing avaient deja ACCOUNTANT configure correctement
- CreateUserDto utilise l'enum AuthUserRole qui inclut ACCOUNTANT

### File List
- Aucune modification necessaire — tout etait deja en place

## QA Results
_To be filled by QA agent_
