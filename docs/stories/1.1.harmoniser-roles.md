# Story 1.1: Harmoniser le systeme de roles

## Status: Ready for Review

## Story

**As a** developpeur,
**I want** que les roles soient identiques entre backend, frontend et mobile,
**so that** les echecs silencieux de verification de permissions soient elimines.

## Acceptance Criteria

1. L'enum `AuthUserRole` backend contient : `SUPERADMIN`, `CLINIC_ADMIN`, `EMPLOYEE`, `PRACTITIONER`, `ACCOUNTANT`
2. L'enum `UserRole` frontend dans `types/index.ts` contient les memes 5 valeurs
3. Le type `UserManagement.role` est mis a jour pour inclure `ACCOUNTANT`
4. Toutes les references a `SUPER_ADMIN`, `STAFF`, `ADMIN` dans le frontend sont remplacees
5. La sidebar utilise les noms de roles exacts du backend
6. Le hook `useAuth.hasRole()` fonctionne avec les nouveaux noms
7. La page `admin/users` affiche et permet de creer le role `ACCOUNTANT`

## Tasks / Subtasks

- [x] **Task 1 : Modifier l'enum backend** (AC: 1) — deja a jour
- [x] **Task 2 : Aligner l'enum frontend** (AC: 2, 3) — deja a jour
- [x] **Task 3 : Rechercher et remplacer les anciennes valeurs** (AC: 4) — supprime 'ADMIN' de sidebar
- [x] **Task 4 : Mettre a jour la sidebar** (AC: 5) — corrige
- [x] **Task 5 : Verifier le hook useAuth** (AC: 6) — verifie OK
- [x] **Task 6 : Mettre a jour la page admin/users** (AC: 7) — ajoute ACCOUNTANT au filtre
- [x] **Task 7 : Verification** (AC: 1-7) — enums alignes

## Dev Notes

### Fichiers a modifier

**Backend :**
- `medicalBackend/src/auth/entities/user.entity.ts` — enum `AuthUserRole` (ligne 6-11)
  [Source: architecture.md#data-models]

**Frontend :**
- `types/index.ts` — enum `UserRole` (ligne 213-218), type `UserManagement.role` (ligne 476)
- `components/layout/sidebar.tsx` — tableau `navigation` roles (lignes 61, 77, 78, 85, 97, 108)
- `hooks/useAuth.ts` — import et usage de `UserRole` (ligne 4, 235)
- `app/admin/users/page.tsx` — schema Zod (ligne 98), SelectContent (lignes 475-479, 638-643), getRoleLabel/getRoleColor (lignes 333-361)

### Valeurs actuelles vs cibles

| Localisation | Actuel | Cible |
|-------------|--------|-------|
| Backend enum | `SUPERADMIN, CLINIC_ADMIN, EMPLOYEE, PRACTITIONER` | + `ACCOUNTANT` |
| Frontend enum | `SUPER_ADMIN, CLINIC_ADMIN, STAFF, PRACTITIONER` | `SUPERADMIN, CLINIC_ADMIN, EMPLOYEE, PRACTITIONER, ACCOUNTANT` |
| Sidebar roles | `SUPERADMIN, CLINIC_ADMIN, ADMIN, EMPLOYEE` | `SUPERADMIN, CLINIC_ADMIN, EMPLOYEE, PRACTITIONER, ACCOUNTANT` |
| Admin/users | `SUPERADMIN, CLINIC_ADMIN, EMPLOYEE, PRACTITIONER` | + `ACCOUNTANT` |

### Attention

- Le backend utilise DEJA `SUPERADMIN` (pas `SUPER_ADMIN`). Le frontend doit s'aligner.
- La sidebar mentionne `'ADMIN'` comme role (ligne 61) — ce role n'existe pas dans le backend, il faut le remplacer par `'SUPERADMIN'`.

### Testing

- **Framework :** Pas de tests unitaires frontend. Verification manuelle.
- **Backend :** Les tests E2E existants dans `medicalBackend/test/auth.e2e-spec.ts` utilisent `AuthUserRole` — ils restent valides.
- **Verification :** Build frontend sans erreur TypeScript. Build backend sans erreur.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-11 | 1.0 | Creation initiale | Bob (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### Debug Log References
Aucun bug rencontre

### Completion Notes List
- Backend enum et frontend enum deja alignes avec ACCOUNTANT
- Supprime role 'ADMIN' invalide de la sidebar
- Ajoute ACCOUNTANT au filtre de roles dans admin/users

### File List
- `components/layout/sidebar.tsx` — supprime 'ADMIN' des roles Praticiens
- `app/admin/users/page.tsx` — ajoute ACCOUNTANT au filtre

## QA Results
_To be filled by QA agent_
