# Story 3.5: Error Boundary global

## Status: Ready for Review

## Story

**As a** utilisateur,
**I want** voir une page d'erreur comprehensible en francais si quelque chose plante,
**so that** je ne sois pas confronte a un ecran blanc sans explication.

## Acceptance Criteria

1. Composant `ErrorBoundary` React ajoute au `MainLayout`
2. En cas d'erreur non geree : icone d'erreur, message "Une erreur inattendue s'est produite", bouton "Recharger la page", bouton "Retour au tableau de bord"
3. L'erreur est loguee dans la console en mode developpement
4. Le composant ne capture pas les erreurs d'authentification

## Tasks / Subtasks

- [ ] **Task 1 : Creer le composant ErrorBoundary** (AC: 1, 2, 3)
  - [ ] Creer `components/layout/error-boundary.tsx`
  - [ ] Utiliser un class component React (les Error Boundaries necessitent `componentDidCatch`)
  - [ ] State : `{ hasError: boolean, error: Error | null }`
  - [ ] `static getDerivedStateFromError(error)` → `{ hasError: true, error }`
  - [ ] `componentDidCatch(error, errorInfo)` → `if (process.env.NODE_ENV === 'development') console.error(error, errorInfo)`
  - [ ] Render en cas d'erreur :
    - Centrer verticalement/horizontalement
    - Icone `AlertTriangle` (Lucide) en rouge, taille 48
    - Titre : "Une erreur inattendue s'est produite"
    - Description : "Nous sommes desoles pour la gene. Veuillez recharger la page ou retourner au tableau de bord."
    - Bouton primaire : "Recharger la page" → `window.location.reload()`
    - Bouton secondaire : "Retour au tableau de bord" → `window.location.href = '/dashboard'`
  - [ ] Utiliser les composants shadcn/ui : Card, Button

- [ ] **Task 2 : Exclure les erreurs d'authentification** (AC: 4)
  - [ ] Dans `getDerivedStateFromError()`, verifier si l'erreur est une erreur d'auth (ex: contient "401" ou "Unauthorized")
  - [ ] Si oui, ne pas capturer → laisser le flow d'auth normal gerer

- [ ] **Task 3 : Integrer dans MainLayout** (AC: 1)
  - [ ] Ouvrir `components/layout/main-layout.tsx`
  - [ ] Wrapper le `{children}` avec `<ErrorBoundary>{children}</ErrorBoundary>`

- [ ] **Task 4 : Verification** (AC: 1-4)
  - [ ] Simuler une erreur dans un composant (ex: acceder a une propriete undefined) → la page d'erreur s'affiche
  - [ ] Cliquer "Recharger" → la page se recharge
  - [ ] Cliquer "Retour au tableau de bord" → navigation vers /dashboard
  - [ ] Verifier qu'en fonctionnement normal, l'ErrorBoundary est invisible

## Dev Notes

### Pourquoi un class component

React Error Boundaries necessitent `componentDidCatch` et `getDerivedStateFromError` qui ne sont pas disponibles avec les hooks. C'est le seul class component du projet.

### Fichier a creer

- `components/layout/error-boundary.tsx`

### Fichier a modifier

- `components/layout/main-layout.tsx` — wrapper children avec ErrorBoundary

### Pattern de code

```typescript
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { AlertTriangle } from 'lucide-react';

interface ErrorBoundaryState {
  hasError: boolean;
  error: Error | null;
}

export class ErrorBoundary extends React.Component<
  { children: React.ReactNode },
  ErrorBoundaryState
> {
  constructor(props: { children: React.ReactNode }) {
    super(props);
    this.state = { hasError: false, error: null };
  }

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    if (process.env.NODE_ENV === 'development') {
      console.error('ErrorBoundary caught:', error, errorInfo);
    }
  }

  render() {
    if (this.state.hasError) {
      return (/* UI d'erreur */);
    }
    return this.props.children;
  }
}
```

### Testing

- **Verification :** Test manuel. Provoquer une erreur → page d'erreur. Boutons fonctionnels. Fonctionnement normal non impacte.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-11 | 1.0 | Creation initiale | Bob (SM) |

## Dev Agent Record

### Agent Model Used
_To be filled by dev agent_

### Debug Log References
_To be filled by dev agent_

### Completion Notes List
_To be filled by dev agent_

### File List
_To be filled by dev agent_

## QA Results
_To be filled by QA agent_
