# Story 2.6: Page Profil utilisateur

## Status: Ready for Review

## Story

**As a** utilisateur connecte (tout role),
**I want** pouvoir voir et modifier mon profil et changer mon mot de passe,
**so that** je puisse gerer mon compte de maniere autonome.

## Acceptance Criteria

1. Nouvelle page `/profile` accessible a tous les roles
2. Affichage des infos : prenom, nom, email, role (lecture seule), tenant (lecture seule)
3. Formulaire de modification : prenom, nom, email (avec validation Zod)
4. Section changement de mot de passe : ancien mot de passe, nouveau, confirmation
5. Nouveau endpoint backend `PATCH /auth/profile` pour la mise a jour du profil
6. Nouveau endpoint backend `POST /auth/change-password` avec validation de l'ancien mot de passe
7. Toast de succes/erreur apres chaque action
8. Lien vers la page profil dans le menu utilisateur (user-nav dropdown dans le header)

## Tasks / Subtasks

- [ ] **Task 1 : Creer les endpoints backend** (AC: 5, 6)
  - [ ] Creer `medicalBackend/src/auth/dto/update-profile.dto.ts` :
    - `firstName` (string, optional, min 1)
    - `lastName` (string, optional, min 1)
    - `email` (string, optional, email format)
  - [ ] Creer `medicalBackend/src/auth/dto/change-password.dto.ts` :
    - `currentPassword` (string, min 8)
    - `newPassword` (string, min 8)
    - `confirmPassword` (string, min 8, must match newPassword)
  - [ ] Ouvrir le `AuthController` ou `UsersController`
  - [ ] Ajouter `@Patch('profile')` protege par `JwtAuthGuard` :
    - Recoit `UpdateProfileDto`
    - Met a jour l'utilisateur via `UsersService.update(req.user.id, dto)`
    - Retourne l'utilisateur mis a jour
  - [ ] Ajouter `@Post('change-password')` protege par `JwtAuthGuard` :
    - Recoit `ChangePasswordDto`
    - Verifie l'ancien mot de passe avec `bcrypt.compare()`
    - Hash le nouveau mot de passe avec `bcrypt.hash()`
    - Met a jour le `passwordHash` en base
    - Retourne `{ message: 'Mot de passe modifie avec succes' }`

- [ ] **Task 2 : Creer la page profil frontend** (AC: 1, 2, 3, 4, 7)
  - [ ] Creer `app/profile/page.tsx`
  - [ ] Section infos : Card avec prenom, nom, email (editables), role et tenant (lecture seule avec Badge)
  - [ ] Formulaire React Hook Form + Zod pour les infos
  - [ ] Section mot de passe : Card separee avec 3 champs (ancien, nouveau, confirmation)
  - [ ] Formulaire Zod avec validation `confirmPassword === newPassword`
  - [ ] Appels API via `apiClient.patch('/auth/profile', data)` et `apiClient.post('/auth/change-password', data)`
  - [ ] Toast Sonner pour succes/erreur

- [ ] **Task 3 : Ajouter le lien profil dans le header** (AC: 8)
  - [ ] Ouvrir `components/layout/user-nav.tsx`
  - [ ] Ajouter un item "Mon Profil" dans le dropdown menu, avant "Se deconnecter"
  - [ ] Lien vers `/profile`

- [ ] **Task 4 : Ajouter /profile aux routes protegees** (AC: 1)
  - [ ] Ouvrir `middleware.ts` et ajouter `/profile` aux `protectedRoutes`
  - [ ] Ouvrir `lib/role-routes.ts` et ajouter `/profile` a TOUS les roles

## Dev Notes

### Fichiers a creer

**Backend :**
- `medicalBackend/src/auth/dto/update-profile.dto.ts`
- `medicalBackend/src/auth/dto/change-password.dto.ts`

**Frontend :**
- `app/profile/page.tsx`

### Fichiers a modifier

**Backend :**
- `medicalBackend/src/auth/controllers/auth.controller.ts` (ou `users.controller.ts`) — 2 nouveaux endpoints

**Frontend :**
- `components/layout/user-nav.tsx` — lien profil
- `middleware.ts` — route protegee
- `lib/role-routes.ts` — route autorisee pour tous les roles

### Pattern de validation mot de passe backend

```typescript
const isValid = await bcrypt.compare(dto.currentPassword, user.passwordHash);
if (!isValid) throw new BadRequestException('Mot de passe actuel incorrect');
const newHash = await bcrypt.hash(dto.newPassword, 10);
await this.usersRepository.update(userId, { passwordHash: newHash });
```

### Pattern de formulaire frontend

Utiliser le meme pattern que les autres formulaires du projet : React Hook Form + Zod + shadcn Form components. [Source: architecture.md#coding-standards]

### Testing

- **Backend :** Test unitaire pour les 2 endpoints (validation DTO, hash, comparaison)
- **Frontend :** Test manuel — modifier le profil, changer le mot de passe, verifier le toast

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-11 | 1.0 | Creation initiale | Bob (SM) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### Debug Log References
N/A

### Completion Notes List
- Created backend DTOs: UpdateProfileDto and ChangePasswordDto
- Added updateProfile and changePassword methods to AuthService
- Added PATCH /auth/profile and POST /auth/change-password endpoints to AuthController
- Created frontend profile page with two cards (profile info + password change)
- Uses React Hook Form + Zod validation with sonner toast notifications
- Added "Mon Profil" link in header dropdown and user-nav dropdown
- Added /profile to protected routes in middleware.ts
- Profile form updates localStorage for immediate UI reflection

### File List
- `medicalBackend/src/auth/dto/update-profile.dto.ts` (NEW)
- `medicalBackend/src/auth/dto/change-password.dto.ts` (NEW)
- `medicalBackend/src/auth/services/auth.service.ts` (MODIFIED)
- `medicalBackend/src/auth/controllers/auth.controller.ts` (MODIFIED)
- `app/profile/page.tsx` (NEW)
- `components/layout/header.tsx` (MODIFIED)
- `components/layout/user-nav.tsx` (MODIFIED)
- `middleware.ts` (MODIFIED)

## QA Results
_To be filled by QA agent_
